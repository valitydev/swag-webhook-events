description: Invoice fulfillment event
allOf:
  - 
    $ref: "../schemas/Event.yaml"
  - 
    type: object
    required:
      - invoice
    properties:
      invoice:
        $ref: "../schemas/Invoice.yaml"
