description: Событие об успешной обработке платежа
allOf:
  - $ref: '#/definitions/Event'
  - type: object
    required:
      - payment
    properties:
      payment:
        $ref: '#/definitions/Payment'
