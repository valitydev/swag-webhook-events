description: Событие об изменении статуса плательщика на "Не готов"
allOf:
  - $ref: '#/definitions/Event'
  - type: object
    required:
      - customer
    properties:
      customer:
        $ref: '#/definitions/Customer'
