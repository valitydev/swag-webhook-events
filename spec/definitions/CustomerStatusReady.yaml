description: Событие об изменении статуса плательщика на "Готов"
allOf:
  - $ref: '#/definitions/Event'
  - type: object
    required:
      - customer
    properties:
      customer:
        $ref: '#/definitions/Customer'
